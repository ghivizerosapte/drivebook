# admin/ — Manual QA panel

## Scope
Static HTML talking to `/v1/admin/*` and booking endpoints for human testing.

## Rules
- No auth in v1 (local/dev only) — do not expose publicly without a gateway
- Cancel booking + list stats + open slots
